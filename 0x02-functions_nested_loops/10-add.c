#include "main.h"
/**
 * add - adss two integers.
 *
 * @num: first number to be added.
 * @num1: second number to be added.
 *
 * Return: results of added numbers.
 */
int add(int num, int num1)
{
	int result = num + num1;

	return (result);
}
