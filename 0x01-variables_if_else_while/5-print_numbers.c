#include <stdio.h>
/**
 * main - A program prints all single numbers of base 10
 *
 * Return: 0 (Success)
 */
int main(void)
{
	int num;

	for (num = 0; num <= 9; num++)
	{
		printf("%d\n", num);
	}
	return (0);
}
